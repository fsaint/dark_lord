package com.fsaint.androidagent.runtime

/** Keeps the agent runtime portable and independent of Android presentation code. */
object RuntimeModuleArchitecture {
    val androidUiDependencies: Set<String> = emptySet()
}
